<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Saat error (custom kalau mau dipakai manual di code) -->
    <item android:state_activated="true">
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <stroke android:width="2dp" android:color="@color/error_red" />
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

    <!-- Saat fokus -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <stroke android:width="2dp" android:color="@color/primaryColor" />
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

    <!-- Default / normal -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <stroke android:width="1dp" android:color="@color/gray_400" />
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

</selector>
